// src/app/join/layout.tsx
"use client";

import { ReactNode } from "react";

export default function JoinLayout({ children }: { children: ReactNode }) {

  return <div className="join-pag">{children}</div>;
}